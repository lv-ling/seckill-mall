# 简易电商秒杀商城 - 最小可行微服务技术方案

> 目标：用**最小功能**完整串联 Spring Cloud Alibaba 全套技术栈，跑通「库存入库 → 秒杀下单 → 扣减库存 → 创建订单」完整流程。  
> 适合前端转后端的学习项目，功能极简，重点在架构串联。

---

## 一、项目定位与目标

### 1.1 项目名称
**seckill-mall**（简易秒杀商城）

### 1.2 核心目标
实现以下**最小闭环流程**即可：

1. 管理员/系统 往库存服务写入商品库存
2. 用户通过网关发起秒杀请求
3. 秒杀服务使用 Redis 预减库存（防超卖）
4. 秒杀成功后通过 RocketMQ 异步通知订单服务创建订单
5. 订单服务创建订单后，再通知库存服务做最终库存扣减（数据库层面）
6. 整个过程所有服务都通过 Nacos 注册发现，统一走 Gateway 入口

### 1.3 不包含的功能（刻意精简）
- 用户注册/登录（先用固定 userId 模拟）
- 商品详情页、购物车、支付、退款
- 后台管理界面
- 复杂的分布式事务（Seata）
- 权限、限流熔断的完整配置（后期再加）

---

## 二、技术选型（推荐稳定版本，2026 学习友好）

| 分类              | 技术                          | 推荐版本                  | 说明 |
|-------------------|-------------------------------|---------------------------|------|
| JDK               | OpenJDK / Oracle JDK          | **17** 或 21              | 最低 17 |
| 构建工具          | Maven                         | 3.9+                      | - |
| 核心框架          | Spring Boot                   | **3.2.12**                | 稳定成熟 |
| 微服务框架        | Spring Cloud                  | **2023.0.3**              | 对应 Boot 3.2 |
| 微服务全家桶      | Spring Cloud Alibaba          | **2023.0.3.2** 或最新 2023.0.x | 官方推荐 |
| 服务注册/配置     | Nacos                         | **2.3.2** 或 2.4.x        | 单机模式即可 |
| 网关              | Spring Cloud Gateway          | 随 Spring Cloud           | - |
| 服务调用          | OpenFeign                     | 随 Spring Cloud           | - |
| 持久层            | MyBatis-Plus                  | **3.5.7** 或更高          | 配合 Spring Boot 3 |
| 数据库            | MySQL                         | **8.0**                   | - |
| 缓存              | Redis                         | **7.x**                   | 单机即可 |
| 消息队列          | Apache RocketMQ               | **5.1.4** 或 5.2.x        | 推荐官方 |
| RocketMQ 集成     | rocketmq-spring-boot-starter  | **2.3.1** 或更高          | 官方 starter |
| 工具库            | Hutool / Lombok / Fastjson2   | 最新稳定                  | 可选 |
| 接口文档          | SpringDoc / Knife4j           | 可选                      | 后期加 |

> **版本选择原则**：优先选 2023.0.x 分支（Boot 3.2），文档多、坑少、社区案例丰富。  
> 不建议直接上 Spring Boot 4 / SCA 2025.x（太新，学习资料少）。

### 依赖管理建议（父 pom 核心）

```xml
<properties>
    <java.version>17</java.version>
    <spring-boot.version>3.2.12</spring-boot.version>
    <spring-cloud.version>2023.0.3</spring-cloud.version>
    <spring-cloud-alibaba.version>2023.0.3.2</spring-cloud-alibaba.version>
    <mybatis-plus.version>3.5.7</mybatis-plus.version>
    <rocketmq-spring-boot.version>2.3.1</rocketmq-spring-boot.version>
</properties>
```

---

## 三、服务拆分（最小 4 个业务服务 + 1 个网关）

| 服务名                  | 端口建议 | 职责（最小）                          | 是否必须 |
|-------------------------|----------|---------------------------------------|----------|
| gateway-service         | 8080     | 统一入口、路由转发                    | 必须 |
| product-stock-service   | 8081     | 商品库存管理（入库、查询、最终扣减）  | 必须 |
| seckill-service         | 8082     | 秒杀核心逻辑（Redis 预减 + 发 MQ）    | 必须 |
| order-service           | 8083     | 订单创建、查询                        | 必须 |
| （可选）common          | -        | 公共实体、DTO、工具类                 | 强烈推荐 |

> 说明：把「商品」和「库存」先合并在一个服务里，降低初期复杂度。后期可再拆。

---

## 四、最小功能需求清单

### 4.1 库存服务（product-stock-service）

| 编号 | 功能点                     | 接口示例                          | 说明 |
|------|----------------------------|-----------------------------------|------|
| S1   | 商品库存入库               | POST /stock/init                  | 初始化商品 + 库存数量 |
| S2   | 查询商品库存               | GET  /stock/{productId}           | 查数据库真实库存 |
| S3   | 扣减库存（最终一致性）     | POST /stock/deduct                | 被订单服务或 MQ 消费者调用 |
| S4   | 预热库存到 Redis           | POST /stock/preload/{productId}   | 把库存加载到 Redis |

### 4.2 秒杀服务（seckill-service）

| 编号 | 功能点                     | 接口示例                          | 说明 |
|------|----------------------------|-----------------------------------|------|
| K1   | 秒杀下单（核心）           | POST /seckill/{productId}         | 用户发起秒杀 |
| K2   | Redis 预减库存             | 内部逻辑                          | 使用 DECR 或 Lua 脚本 |
| K3   | 发送秒杀成功消息到 MQ      | 内部逻辑                          | 发送到 RocketMQ |

### 4.3 订单服务（order-service）

| 编号 | 功能点                     | 接口示例                          | 说明 |
|------|----------------------------|-----------------------------------|------|
| O1   | 创建订单（被 MQ 消费）      | 内部 MQ 消费者                    | 消费秒杀成功消息 |
| O2   | 查询订单                   | GET  /order/{orderId}             | 简单查询 |
| O3   | 查询用户订单列表           | GET  /order/user/{userId}         | 可选 |

### 4.4 网关（gateway-service）

- 路由规则：
  - `/api/stock/**` → product-stock-service
  - `/api/seckill/**` → seckill-service
  - `/api/order/**` → order-service
- 暂时不做鉴权、限流（后期加）

---

## 五、核心业务流程（必须跑通的最小路径）

```
1. 初始化库存
   调用库存服务 → 写入 MySQL（商品表 + 库存表）
   调用库存服务 → 把库存数量预热到 Redis（key: seckill:stock:{productId}）

2. 用户秒杀
   客户端 → Gateway → 秒杀服务
   秒杀服务：
     a. Redis DECR 预减库存（成功才继续）
     b. 构造秒杀成功消息（userId + productId + 数量）
     c. 发送到 RocketMQ Topic（seckill-success-topic）
     d. 立即返回“秒杀成功，订单生成中”

3. 异步创建订单 + 最终扣库存
   订单服务消费 MQ 消息：
     a. 创建订单记录（MySQL）
     b. 调用库存服务扣减数据库真实库存（或再发一个扣库存消息）
     c. （可选）更新订单状态

4. 验证
   - 查询订单是否生成
   - 查询数据库库存是否正确减少
   - 查询 Redis 库存是否同步
```

### 防超卖关键点（最小实现）
- Redis 预减：使用 `DECR`，如果结果 < 0 则回滚并返回失败
- 数据库最终扣减：使用 `UPDATE stock SET quantity = quantity - 1 WHERE product_id = ? AND quantity > 0`

---

## 六、数据库设计（极简）

### 6.1 库存服务库（seckill_stock）

```sql
-- 商品表
CREATE TABLE product (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    name        VARCHAR(100) NOT NULL,
    price       DECIMAL(10,2) NOT NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 库存表
CREATE TABLE stock (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    product_id  BIGINT NOT NULL UNIQUE,
    quantity    INT NOT NULL DEFAULT 0,          -- 剩余库存
    version     INT NOT NULL DEFAULT 0,          -- 乐观锁（可选）
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 6.2 订单服务库（seckill_order）

```sql
CREATE TABLE orders (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no    VARCHAR(64) NOT NULL UNIQUE,     -- 订单号
    user_id     BIGINT NOT NULL,
    product_id  BIGINT NOT NULL,
    quantity    INT NOT NULL DEFAULT 1,
    status      TINYINT NOT NULL DEFAULT 0,      -- 0-待支付 1-已创建 2-已取消
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

> 初期可以先共用一个数据库，后期再按服务拆库。

---

## 七、RocketMQ Topic / 消费组规划

| Topic 名称                  | 生产者           | 消费者           | 用途 |
|-----------------------------|------------------|------------------|------|
| seckill-success-topic       | seckill-service  | order-service    | 秒杀成功通知创建订单 |
| stock-deduct-topic（可选）  | order-service    | stock-service    | 订单创建后最终扣库存 |

消息体示例（JSON）：
```json
{
  "userId": 1001,
  "productId": 1,
  "quantity": 1,
  "seckillTime": 1724000000000
}
```

---

## 八、Redis Key 设计

| Key 模式                      | 类型   | 说明 |
|-------------------------------|--------|------|
| seckill:stock:{productId}     | String | 库存数量（预减用） |
| seckill:order:{userId}:{productId} | String | 用户是否已秒杀过该商品（防重复） |

---

## 九、项目结构建议（Maven 多模块）

```
seckill-mall
├── pom.xml                          # 父工程
├── seckill-common                   # 公共模块（实体、DTO、工具、常量）
├── gateway-service                  # 网关
├── product-stock-service            # 库存服务
├── seckill-service                  # 秒杀服务
└── order-service                    # 订单服务
```

---

## 十、本地环境安装顺序建议

1. **JDK 17**
2. **Maven 3.9+**
3. **MySQL 8.0**（创建两个库或一个库）
4. **Redis 7.x**
5. **Nacos 2.3.2**（单机模式，`startup.sh -m standalone`）
6. **RocketMQ 5.1.4**（NameServer + Broker）
7. 创建父工程 + 各子模块
8. 依次启动：Nacos → Redis → MySQL → RocketMQ → 各微服务 → Gateway

---

## 十一、验收标准（跑通即成功）

1. 能通过库存服务接口成功初始化商品库存，并预热到 Redis
2. 调用秒杀接口后，Redis 库存正确减少
3. RocketMQ 消息被订单服务成功消费，订单表有记录
4. 数据库库存最终被正确扣减
5. 所有服务都能在 Nacos 控制台看到注册信息
6. 所有请求都通过 Gateway 访问

---

## 十二、后续可扩展方向（学完最小版本后再加）

- 用户服务 + JWT 鉴权
- Sentinel 限流熔断
- 分布式锁（Redisson）
- 延迟消息处理订单超时取消
- Seata 分布式事务（可选）
- Docker Compose 一键启动中间件
- SkyWalking 链路追踪

---

**文档版本**：v1.0  
**适用对象**：前端转 Java 微服务学习者  
**核心原则**：先跑通最小闭环，再逐步加功能

确认本方案后，下一步我可以继续给你：
1. 父工程 + 各模块的完整 pom.xml 模板
2. 各服务的基础目录结构
3. 中间件安装详细步骤（Nacos / Redis / RocketMQ / MySQL）
```

